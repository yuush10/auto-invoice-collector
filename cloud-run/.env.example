# Cloud Run Local Development Environment Variables
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local

# =============================================================================
# AI Login Configuration (Issue #70)
# =============================================================================

# Enable AI-driven login automation (default: false)
ENABLE_AI_LOGIN=false

# Anthropic API key for Claude (required if ENABLE_AI_LOGIN=true)
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Timeout for AI login attempt in milliseconds (default: 60000)
AI_LOGIN_TIMEOUT=60000

# Fall back to manual login if AI fails (default: true)
AI_LOGIN_FALLBACK=true

# =============================================================================
# Vendor Credentials
# =============================================================================

# IBJ credentials (for testing)
IBJ_USERNAME=your-ibj-username
IBJ_PASSWORD=your-ibj-password

# =============================================================================
# Google Cloud Configuration (optional for local development)
# =============================================================================

# GCP Project ID (for Secret Manager access)
# GOOGLE_CLOUD_PROJECT=your-project-id

# Path to service account key (for local auth)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
